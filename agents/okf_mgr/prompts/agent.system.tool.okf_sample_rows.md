### okf_sample_rows:
Sample rows from a BigQuery table concept. Mirrors the reference agent's `sample_rows` capability.

Args: `dataset` (`project.dataset`), `concept_id` such as `tables/users`, optional `n`, `billing_project`.
