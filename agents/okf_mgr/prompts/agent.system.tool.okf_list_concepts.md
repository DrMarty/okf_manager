### okf_list_concepts:
List concepts from the active OKF bundle or BigQuery dataset. Mirrors the reference agent's `list_concepts` capability.

Args: optional `source` (`bundle` or `bq`), `bundle_root`, `dataset`, `billing_project`.
