### okf_regenerate_indexes:
Generate reference-agent-style `index.md` files for each populated directory in an OKF bundle, grouping child concepts by `type` and linking to child indexes.

Args: optional `bundle_root`.
