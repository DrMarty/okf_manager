### okf_read_concept_raw:
Read raw metadata for one concept. For `source: "bundle"`, returns the concept markdown. For `source: "bq"`, returns BigQuery dataset/table metadata including schema where available.

Args: `concept_id`, optional `source`, `bundle_root`, `dataset`, `billing_project`.
