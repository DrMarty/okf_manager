### okf_read_existing_doc:
Read an existing OKF concept document from a bundle and return JSON with `frontmatter` and `body`, or `null` when absent. Use before augmenting documents.

Args: `concept_id`, optional `bundle_root`.
